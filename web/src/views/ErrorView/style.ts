import { makeStyles } from '@material-ui/core/styles'

const useStyle = makeStyles({
  errorIcon: {
    color: "white",
    fontSize: "204px"
  },
  errorText: {
    color: "white",
    fontSize: "32px"
  }
});

export default useStyle;