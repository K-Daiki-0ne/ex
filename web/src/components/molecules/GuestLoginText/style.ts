import { makeStyles } from '@material-ui/core/styles'

const useStyle = makeStyles({
  guestLoginText: {
    color: "#E8EAED"
  },
  guestLoginLinkText: {
    textDecoration: "none",
    color: "#e57373"
  }
});

export default useStyle;