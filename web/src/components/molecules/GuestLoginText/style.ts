import { makeStyles } from '@material-ui/core/styles'

const useStyle = makeStyles({
  guestLoginText: {
    color: "#E8EAED"
  },
  guestLoginLinkText: {
    textDecoration: "none",
    color: "#e57373",
    cursor: "pointer"
  },
  alertTitle: {
    textAlign: "left"
  }
});

export default useStyle;