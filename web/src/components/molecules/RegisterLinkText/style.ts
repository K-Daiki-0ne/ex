import { makeStyles } from '@material-ui/core/styles'

const useStyle = makeStyles({
  registerText: {
    color: "#E8EAED"
  },
  registerLinkText: {
    textDecoration: "none",
    color: "#e57373"
  }
});

export default useStyle;